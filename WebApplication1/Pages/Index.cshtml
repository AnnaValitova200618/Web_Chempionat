@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}
@if (!Model.Auth)
{
    <form method="post">
        <div class="text-center" >
            <h1 class="display-4">@Model.SomeString</h1>
            <h4 >Логин</h4>
            <input type="text" name="login"/>
            <h4>Пароль</h4>
            <input type="password" name="password"/>
        </div>
        <div class="row mt-3"> <div class="col d-flex justify-content-center">
        <div class="btn-group m-2" role="group">
            
                <button type="submit" value="Войти" class="btn btn-primary">Войти</button>
                </div>
                 <div class="btn-group m-2" role="group">
                    <a class="btn btn-primary" asp-page="/Registr" >Зарегестрироваться</a>
                </div>
        </div>
        </div>
        <h4 class="text-center">@Model.Message</h4>
        
        
    </form>
}
